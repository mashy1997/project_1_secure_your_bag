{% extends "base.jinja" %}
{% block content %}

<form action="/customer_transactions/{{customer_transaction.id}}" method="'post">
    <div>
        <label for="description">Description</label>
        <input type="text" id="description" name="description" value="{{customer_transaction.description}}" required autofocus>
    </div>

    <div>
        <label for="amount">Amount</label>
        <input type="number" id="amount" name="amount" value="{{amount.id}}" required>
    </div>

    <div>
        <label for="merchant">Merchant</label>
        <input type="merchant" id="merchant" name="merchant" value="{{merchant.id}}" required>
        </select>
    </div>

    <button type="submit">Edit Transaction</button>

</form>
{% endblock %}